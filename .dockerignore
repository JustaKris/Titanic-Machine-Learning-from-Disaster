# ===========================================
# Allowlist approach: exclude everything,
# then include only what the Docker build needs.
# ===========================================

# Exclude everything by default
*

# Include files needed for dependency installation (Stage 1: Builder)
!pyproject.toml
!uv.lock

# Include application code and model artifacts (Stage 2: Runtime)
!src/
!saved_models/